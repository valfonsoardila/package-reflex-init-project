[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
reflex = "^0.5.9"

[tool.poetry.scripts]
create-skln = "skln:main"
test = "pytest"
